import ingCat from '../../ingCat';
const initialState = {
  ingCat: ingCat,
};
const reducer = (state = initialState, action) => {
  switch (action.type) {
    default:
      return state;
  }
};

export default reducer;
