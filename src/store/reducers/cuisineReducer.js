import cuisines from '../../cuisine';
const initialState = {
  cuisines: cuisines,
};
const reducer = (state = initialState, action) => {
  switch (action.type) {
    default:
      return state;
  }
};

export default reducer;
